helm repo add traefik https://traefik.github.io/charts; helm repo update;

helm install traefik traefik/traefik --namespace traefik --create-namespace --set service.type=NodePort --set service.spec.externalTrafficPolicy=Cluster --set ports.web.nodePort=30080 --set ports.websecure.nodePort=30443;


