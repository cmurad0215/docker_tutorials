sudo docker run -d -p 5000:5000 --restart always --name registry muraddevops/registry_v2;

sudo docker run -d -e ENV_DOCKER_REGISTRY_HOST=172.16.10.155 -e ENV_DOCKER_REGISTRY_PORT=5000 -p 8080:80 --restart always muraddevops/konradkleine_docker_registry_frontend;


#do not use localhost IP, the webportal will fail to load
#for insecure (without https, not recommended for live servers, only for test servers) access to docker registry, check the file docker_daemon.json
